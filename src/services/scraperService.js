// ScraperService: Scrapes AppSumo URLs for metadata/images

class ScraperService {
  async fetchProductData(appsumoUrl) {
    // TODO: Implement scraping logic (axios + cheerio)
    return {
      productName: '',
      featuredImageUrl: ''
    };
  }
}

module.exports = ScraperService;
